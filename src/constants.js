export const ORDER_STATUS = Object.freeze({
    pending: "PENDIENTE",
    delivered: "SERVIDA",
});

export const ORDER_STATUS_PARAMS = Object.freeze({
    pending: "pending",
    delivered: "delivered",
    all: "all",
});